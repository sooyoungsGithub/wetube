mixin videoBlock(video = {})
    .videoBlock 
        video.videoBlcock__thumbnail(src=video.videoFile, controls=true)
        h4.videoVlock__title=video.title
        h6.videoBlock__views=video.views